import { StyleSheet } from 'react-native';

export const hoodStyles = StyleSheet.create({
  hud: {
    height: 76,
    paddingHorizontal: 18,
    paddingTop: 14,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
